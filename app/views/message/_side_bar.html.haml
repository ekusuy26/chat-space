.side-bar
  .side-bar__side-header
    .side-bar__side-header__box
      .side-bar__side-header__box__bag
        = current_user.nickname
      .side-bar__side-header__box__menu
        = link_to "groups/new", class: "btn" do
          = icon('far', 'edit')
          %span<>
        = link_to edit_user_path(current_user), class: "btn" do
          = icon('fas', 'cog')
          %span<>
  .side-bar__groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__group-name
            = group.name
          .group__message
            メッセージはまだありません。
    .side-bar__groups__group
      .side-bar__groups__group__group-name
        techcamp
      .side-bar__groups__group__message
        まだメッセージはありません